<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 둥근 모서리 공통 -->
    <style name="MyButtonShape">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>

    <!-- Outlined 버튼 -->
    <style name="MyOutlinedButton" parent="Widget.Material3.Button.OutlinedButton">
        <item name="shapeAppearance">@style/MyButtonShape</item>
        <item name="android:textColor">@color/blue</item>
    </style>

    <!-- Filled 버튼 -->
    <style name="MyFilledButton" parent="Widget.Material3.Button">
        <item name="shapeAppearance">@style/MyButtonShape</item>
        <item name="backgroundTint">@color/blue</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- 머터리얼3 다이얼로그용 오버레이 -->
    <style name="ThemeOverlay.MyApp.AlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="materialAlertDialogTitleTextStyle">@style/AlertTitleText</item>
        <item name="materialAlertDialogBodyTextStyle">@style/AlertBodyText</item>
    </style>

    <style name="AlertTitleText" parent="@style/TextAppearance.Material3.TitleLarge">
        <item name="android:textColor">?attr/colorOnSurface</item>
    </style>

    <style name="AlertBodyText" parent="@style/TextAppearance.Material3.BodyMedium">
        <item name="android:textColor">?attr/colorOnSurfaceVariant</item>
    </style>

    <style name="FilterButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">36dp</item>
        <item name="android:minWidth">72dp</item>
        <item name="android:paddingHorizontal">14dp</item>
        <item name="android:textColor">#222222</item>
        <item name="android:background">@drawable/bg_filter_white</item>
        <item name="android:backgroundTint">@null</item>
        <item name="android:textSize">14sp</item>
    </style>
</resources>
