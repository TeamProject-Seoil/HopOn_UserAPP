<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    android:foreground="?android:attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="12dp">

        <!-- 버스 번호 박스 -->
        <TextView
            android:id="@+id/bus_number_box"
            android:layout_width="64dp"
            android:layout_height="48dp"
            android:gravity="center"
            android:background="@drawable/bg_bus_number"
            android:text="320"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:textSize="20sp" />

        <!-- 노선 정보 -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/bus_name"
                android:text="울마삼 → 사가정"
                android:textStyle="bold"
                android:textColor="#333333"
                android:textSize="14sp"
                android:layout_marginBottom="2dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/bus_info"
                android:text="평일 운행 / 배차간격 10분"
                android:textColor="#666666"
                android:textSize="12sp" />
        </LinearLayout>

        <!-- 별 아이콘 -->
        <ImageView
            android:id="@+id/fav_icon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_star2"
            android:contentDescription="즐겨찾기 해제"
            app:tint="#FFB300" />
    </LinearLayout>
</androidx.cardview.widget.CardView>
